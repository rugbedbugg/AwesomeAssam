---
name: Bug report
about: Something in the pipeline reports the wrong thing, or breaks
title: "[bug] "
labels: []
---

## Environment

- OS:
- Python version (`python --version`):
- Install method (`mise run install` / `uv sync --locked`):
- Commit or version (`git rev-parse --short HEAD`):
- Inference runtime installed? (PyTorch + AI4Bharat NeMo, yes/no):

## Command

```bash
# the exact command you ran (redact local paths if needed)
```

## Expected behaviour

## Actual behaviour

## Logs / output

```
paste the output, including the exit code
```

## Anything else

- Does it reproduce with synthetic audio and a minimal manifest? If yes, please
  include that manifest. Do **not** attach private recordings.
- If a metric looks wrong, include the reference and hypothesis strings
  (Assamese text is fine; keep them short).